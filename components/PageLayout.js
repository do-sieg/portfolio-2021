// Composant head

export default function PageLayout({ children }) {
    return (
        <>
            <header>HEADER</header>
            {children}
            <footer>FOOTER</footer>
        </>
    );
}
